const sideBar = document.getElementById("side-bar");
const mainContainer = document.getElementById("main-container");
const body = document.querySelector("body");

export { sideBar, mainContainer, body };
